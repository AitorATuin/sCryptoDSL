package com.logikujo.cryptoI

import javax.crypto.Cipher
import javax.crypto.spec.SecretKeySpec

object Week2 {
//  def pkcs5Pad(text: Array[Byte]): Array[Byte] = pkcs5Pad(text, 16)
  /**
   * Pad this block in the same way as pkcs5Pad but with a selectable block size.
   */
/*  def pkcs5Pad(text:Array[Byte], n: Int):Array[Byte] = {
    val p = n - text.length % n
    text ++ (1 to p).map(i => p.toByte)
  }*/

}
